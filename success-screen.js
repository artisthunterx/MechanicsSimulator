var successScreen = {};

window.addEventListener('load', function() {
    // retrieves the success screen div when window is loaded 
	var successScreenDiv = document.get
	var successScreenDiv = document.getElementById('success-screen');

	successScreenDiv.addEventListener('click', function() {
        // goes to success sreen when div is clicked
		program.goToStartScreen();
	});

	successScreen.show = function() {
		
	};
});